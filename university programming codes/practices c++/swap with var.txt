#include <iostream>
#include <conio.h>
using namespace std;

void swap(int *a,int *b)
{
	*a = *a + *b;
	*b = *a - *b;
	*a = *a - *b;
	cout << "val is a " << *a << endl;
	cout << "val is b " << *b << endl;
	cout << "add is a " << a << endl;
	cout << "add is b " << b << endl;
}

void main()
{
	int a = 100, b = 200;
	swap(&a,&b);
	getch();
}